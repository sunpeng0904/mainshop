## Sheet1

| 序号 | 标题 | 问题描述 | 解决方案 | 录入人 | 修改人 | 录入日期 | 修改日期 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 后端禁用代码 | 开发人员为了解决问题，使用了修改程序运行环境的代码导致其他功能报错 | 禁用对于程序运行环境有影响的代码
System.setProperty(**,**);以及TimeZone.setDefault(**); 等等 |  |  |  |  |
| 2 | kingbase数据库分页查询注意事项 | 1.分页查询时第一页有的内容在第二页也出现了。
2.空值默认要排在最后 | 在 `order by` 条件后 加上主键排序。
 ORDER BY  "s_time" ASC NULLS LAST, "id" ASC |  |  |  |  |
| 3 | kingbase 数据库sql查询注意事项 | kingbase查询 对于 not in 、!= 、not exits 等函数要特别关注 null 值。因为null值的数据是查不出来的。 | 1.查询时加上Or 字段 is null 条件。
2.数据库层面以及java代码层面设置字段值默认为’’(空串)。【不建议】 |  |  |  |  |
| 4 | 诚信报告ftl模板特殊字符处理 | 针对于<、>、\、$等特殊字符，ftl模板在生成html页面时会报错。 | 通过加?html 处理。如：${(report.ins_name?html)} |  |  |  |  |
| 5 | 业务字典集合排序失效 | 前段业务字典下拉选项采用了Map格式。后端map格式排序没问题，但是通过json转换后到前端map中的数据会乱序。 | 前段字典项下拉样式采用list格式。列表等仅需展示的统一由后端转换好后再传给前段。 |  |  |  |  |
| 6 | 修改详情列表类数据主键处理逻辑 | 一个人员有多个职务，用户修改了其中的一条数据，其他数据和本数据主键是否重新生成。重新生成可能会导致数据治理时找不到之前的数据。 | 将之前数据删除后判断传输的列表数据主键字段是否存在，存在的话不修改主键。不存在的话重新生成主键。 |  |  |  |  |
| 7 | 修改详情的列表数据，列表数据排序问题处理 | 保存后展示的列表排序和保存前不一致 | 对应保存详情列表的数据，数据库中添加排序字段。排序字段的内容后端控制（用下标）。避免统一数据详情对应数据的展示，出现排序不一致问题。 |  |  |  |  |
| 8 | Mybatis属性字符串判断无效问题 | Mapping文件中代码编写错误导致的功能异常 | <if   test='doExt.ipoAuthFlag == "1"   or (doExt.userIdIntvList != null and doExt.userIdIntvList.size() > 0)  '> |  |  |  |  |
| 9 | 文件交换目录命名规则 | 文件交换目录命名规则 | 内网 NSIT  证联网 ZSIT   证联网云 ZSITY    互联网 HSIT
互联网到证联网样例如下：
/gfs/arrc/HSIT_ZSIT/XXX
证联网到互联网样例如下：
/gfs/arrc/ZSIT_HSIT/XXX |  |  |  |  |
| 10 | 外围接口调用安全加固 | 外围接口调用安全加固 | 接口调用，通过Key的方式。Key要有失效时间。避免出现旧token值始终有效，未设置注销的漏洞。或者采用对称式加密等其他方式处理。 |  |  |  |  |
| 11 | 富凯互联网不同系统调用报证书失效错误 | 富凯互联网不同系统调用报证书失效错误 | 需联系应用专员（和李东岳沟通）调用方服务器需要安装安全证书。 |  |  |  |  |
| 12 | 不同环境服务调用协议 | 不同环境服务调用协议 | 互联网https  
证联网http
内网 http |  |  |  |  |
| 13 | 接口大概率出现拿不到用户信息问题 | 新仿真接口出现大概率，权限单点验证过去了，但是拿不到用户信息以及用户菜单等情况。本地没有问题。
经排查：新仿真nginx的 /etc/sysctl.conf 中的
net.ipv4.tcp_tw_recycle=1
此配置表示开启TCP连接中TIME-WAIT sockets的快速回收，默认为0，表示关闭。
解释：当多个客户端通过nat（网络地址转换）方式联网，并与服务端交互时，服务端看到的是同一个ip，由于客户端的时间戳可能存在差异，于是从服务端来看会出现时间戳错乱的情况导致时间戳小的数据包被丢失。 | net.ipv4.tcp_tw_recycle=0 |  |  |  |  |
| 14 | tongweb相同代码会仿真能起来会生产启动报错 | 报错信息：缺少XX类。
分析：仿真和生产tongweb版本不一致。生产的是修复安全漏洞的tongweb版本，此版本有问题。 | 使用和仿真同一版本tongweb |  |  |  |  |
| 15 | Kingbases数据插入报错0X00 | ERROR invalid byte sequence for encoding “UTF8”:0x00 | 修改数据库配置
  进入应用数据库 执行以下命令：
 alter system set ignore_char_null_check = on; |  |  |  |  |
| 16 | Excel文件导出有换行标签的处理 | 页面展示的数据中换行是 <br>  </br> 这两种换行标签，展示时浏览器进行转换换行的处理，页面展示不会出现此类标签。 | 导出功能：导出的Excel列中需要将此类换行符替换掉，不展示出来。
if (StringUtils.isNotBlank(sysCueTrblCntnt) && sysCueTrblCntnt.contains("<br>")) {

    ext.setSysCueTrblCntnt(sysCueTrblCntnt.replace("<br>",""));
} |  |  |  |  |
| 17 | MyBatis的like模糊查询含有特殊字符（_、/、%、+） | 模糊搜索字段中有_、/、%、+这几种字符模糊查询失效 | /**
 * 斜杠
 */
public static final String STR_SLASH             =  "/";

/**
 * 双斜杠斜杠
 */
public static final String STR_TWO_SLASH             =  "//";

/**
 * 下划线
 */
public static final String STR_UNDER_LINE             =  "_";
/**
 *  斜杠下划线
 */
public static final String STR_SLASH_UNDER_LINE             =  "/_";
/**
 * 百分号
 */
public static final String STR_PERCENT             =  "%";
/**
 *  斜杠百分号
 */
public static final String STR_SLASH_PERCENT             =  "/%";

/**
 * 加号
 */
public static final String STR_ADD             =  "+";
/**
 *  斜杠加号
 */
public static final String STR_SLASH_ADD             =  "/+";

代码：
if(StringUtils.isNotBlank(keyWord)){
    // 模糊搜索处理 /
    if(keyWord.contains(CommonConstants.STR_SLASH)){
        keyWord =keyWord.replaceAll(CommonConstants.STR_SLASH,CommonConstants.STR_TWO_SLASH);
    }
    // 模糊搜索处理 下划线
    if(keyWord.contains(CommonConstants.STR_UNDER_LINE)){
        keyWord = keyWord.replaceAll(CommonConstants.STR_UNDER_LINE,CommonConstants.STR_SLASH_UNDER_LINE);
    }
    // 模糊搜索处理  百分号
    if(keyWord.contains(CommonConstants.STR_PERCENT)){
        keyWord =keyWord.replaceAll(CommonConstants.STR_PERCENT,CommonConstants.STR_SLASH_PERCENT);
    }
    // 模糊搜索处理 +号
    if(keyWord.contains(CommonConstants.STR_ADD)){
        keyWord =keyWord.replaceAll(CommonConstants.STR_ADD,CommonConstants.STR_SLASH_ADD);
    }
    testCaseBscListQO.setKeyWord(keyWord);
}
Mybatis.xml文件处理
<!-- 关键字  用例标题、前置条件、测试步骤、预期结果  -->
<if test="testCaseBscListQO.keyWord!=null and testCaseBscListQO.keyWord!=''">
    and (
        testCaseBscInfo.case_title like concat('%',#{testCaseBscListQO.keyWord},'%') escape '/' or
        testCaseBscInfo.prps_cndt like concat('%',#{testCaseBscListQO.keyWord},'%') escape '/'  or
        testCaseBscInfo.test_stp like concat('%',#{testCaseBscListQO.keyWord},'%') escape '/'  or
        testCaseBscInfo.pre_prd_result  like concat('%',#{testCaseBscListQO.keyWord},'%') escape '/'
    )
</if> |  |  |  |  |
| 18 | tongweb事务失效问题 | 非分布式事务中，前一个更新执行成功，后一个更新执行失败抛出异常，事务未回滚。（系统所有模块皆有此问题） | Tongweb配置问题。
进入tongweb的bin目录下。修改external.vmoptions 的-DWebModuleOnly配置。
vim external.vmoptions  
-DWebModuleOnly=false  改为   -DWebModuleOnly=true |  |  |  |  |
| 19 | feign请求超时 | 问题：用户同步通过feign调用的权限系统，当用户超过400时，云仿真和云生产会出现hystrix熔断连接超时异常，导致用户同步失败。 
分析：
根据查看源码发现平台脚手架中默认熔断是开启的。默认连接超时时间默认是1s比较短,导致同步的数据量较大时，数据读取超时。 | 项目配置文件中新增超时配置如下：
#feign连接超时时间 单位毫秒
feign.client.config.default.connectionTimeout=60000
#feign读取超时时间 单位毫秒
feign.client.config.default.readTimeout=60000
#openfeign使用的hystrix的超时时间设置 单位毫秒 
hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds=300000
注意：项目中都没有用到nacos（或者ribbon+Eureka）的话，hystrix配置feign.hystrix.enabled可以配置为false，停用熔断，因为本质上没有用到客户端的负载均衡。 |  |  |  |  |
| 20 | Kingbase时间类型默认值 | 问题描述：Erwin生成时间格式的默认值'0001-01-01 00:00:00'：
   "fxd_tm_task_e_time" TIMESTAMP(0) without time zone DEFAULT '0001-01-01 00:00:00'::TIMESTAMP WITHOUT TIME ZONE NOT NULL 
此时通过mybatisPlus查出此字段，然后将此字段'0001-01-01 00:00:00'默认值再存入数据库时，jdbc入库会报错。 | 解决：默认值 NOW() ：
脚本改为：
 "fxd_tm_task_e_time" TIMESTAMP(0)  without time zone DEFAULT NOW() NOT NULL |  |  |  |  |
| 21 | 越过代码扫描NOSONAR问题 | 问题描述：代码中存在//NOSONAR以及@SuppressWarning匹配的(("find\|"java\|"fb\|\{"java\|\{"fb\|\{"find)这几个关键字，导致相关代码会越过代码扫描。
例如：encode(privateKey.getEncoded()))};//NOSONAR
@SuppressWarnings("fb-contrib:USFW_UNSYNCHRONIZED_SINGLETON_FIELD_WRITES")
测试匹配脚本：
egrep -r --include "*.java" 'NOSONAR\|SuppressWarnings\(("find\|"java\|"fb\|\{"java\|\{"fb\|\{"find)' . > sonarRulesCheck_Java.log
egrep -r --include "*.js" 'NOSONAR\|SuppressWarnings\(("find\|"java\|"fb\|\{"java\|\{"fb\|\{"find)' . > sonarRulesCheck_JavaScript.log | 代码中不允许有//NOSONAR以及@SuppressWarning匹配的(("find\|"java\|"fb\|\{"java\|\{"fb\|\{"find)这几个关键字的写法。 |  |  |  |  |
| 22 | mockJs本地下载失效问题解决 | mock.js在拦截XML HttpRequest请求时，会修改原生XHR的行为，导致设置的responseType属性被覆盖为字符串。 | 解决：重写mock.js的Mock.XHR.prototype.send方法
import Mock from 'mockjs'
//  重写 mock.js 的send方法 
Mock.XHR.prototype.send = (()=> {
  const _send = Mock.XHR.prototype.send
  return function(){
    if(!this.match){
      this.custom.xhr.responseType =this.responseType \|\| ''
      this.custom.xhr.timeout =  this.timeout \|\| 0
      this.custom.xhr.withCredentials  = this.withCredentials \|\| false
    }
    return _send.apply(this,arguments)
  }
})() |  |  |  |  |
| 23 | 数据库时间戳查询失效 | 数据库中默认创建的时间戳是TIMESTAMP WITHOUT TIME ZONE，此类型没有精度，实际存储有毫秒值，代码查询时对象用的TIMESTAMP字段，进行了格式化展示的是年月日时分秒 @JsonFormat(timezone = "Asia/Shanghai", pattern = "yyyy-MM-dd HH:mm:ss")此时用此字段进行数据库查询会匹配不上。 | 方案一：去掉 @JsonFormat(timezone = "Asia/Shanghai", pattern = "yyyy-MM-dd HH:mm:ss")注解，技术字段不做任何格式处理。
方案二：将数据库时间戳类型设置精度TIMESTAMP(0) without time zone。 这样数据库存储和前端接收内容一致，就是年与日时分秒了。 |  |  |  |  |
| 24 | kingbase字段bit类型存储报错 | kingbase字段bit类型存储报错 | bit类型改为varchar |  |  |  |  |
| 25 | Excel导出创建临时目录无权限 | excel导出功能提示创建临时目录无权限的错误。
cras服务的jar包启动时，初次启动使用ppss账户，在导出时会在/tmp目录下创建poifiles目录，等到后面cras采用tongweb账户启动后，导出功能报错，会显示创建文件异常。 | 将用ppss用户创建的/tmp删掉，用tongweb用户启动后再执行导出。 |  |  |  |  |
