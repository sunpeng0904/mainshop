# 测试用例

## 基本信息

- **模块**: 地址管理
- **编写时间**: 2026-05-29
- **用例总数**: 25

---

## F001 - 获取地址列表

### TC001: 缓存命中获取地址列表
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001已有地址数据，Redis缓存存在 |
| 测试步骤 | 1. 调用 GET /api/address/list |
| 预期结果 | 返回地址列表，不查询数据库 |
| 优先级 | P0 |

### TC002: 缓存未命中获取地址列表
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001已有地址数据，Redis缓存不存在 |
| 测试步骤 | 1. 清除缓存 2. 调用 GET /api/address/list |
| 预期结果 | 查询数据库，返回地址列表，写入缓存 |
| 优先级 | P0 |

### TC003: 用户无地址数据
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-003无地址数据 |
| 测试步骤 | 1. 调用 GET /api/address/list |
| 预期结果 | 返回空列表 |
| 优先级 | P1 |

---

## F002 - 获取地址详情

### TC004: 获取存在的地址详情
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001存在 |
| 测试步骤 | 1. 调用 GET /api/address/test-addr-001 |
| 预期结果 | 返回地址详情，包含完整地址信息 |
| 优先级 | P0 |

### TC005: 获取不存在的地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址not-exist不存在 |
| 测试步骤 | 1. 调用 GET /api/address/not-exist |
| 预期结果 | 返回错误：address.not.found |
| 优先级 | P0 |

### TC006: 获取他人地址详情
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001属于user-001 |
| 测试步骤 | 1. 使用user-002调用 GET /api/address/test-addr-001 |
| 预期结果 | 返回错误：address.not.found |
| 优先级 | P0 |

---

## F003 - 添加地址

### TC007: 添加第一个地址（自动设为默认）
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-003无地址 |
| 测试步骤 | 1. 调用 POST /api/address/add 2. 传入地址数据 |
| 预期结果 | 添加成功，dft_indc=Y |
| 优先级 | P0 |

### TC008: 添加非默认地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001已有默认地址 |
| 测试步骤 | 1. 调用 POST /api/address/add 2. dftIndc=N |
| 预期结果 | 添加成功，dft_indc=N |
| 优先级 | P0 |

### TC009: 添加默认地址（覆盖原默认）
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001已有默认地址 |
| 测试步骤 | 1. 调用 POST /api/address/add 2. dftIndc=Y |
| 预期结果 | 新地址为默认，原默认地址变为非默认 |
| 优先级 | P0 |

### TC010: 手机号格式验证失败
| 项目 | 内容 |
|------|------|
| 前置条件 | 无 |
| 测试步骤 | 1. 调用 POST /api/address/add 2. rcvrTel=12345678901 |
| 预期结果 | 返回验证错误 |
| 优先级 | P0 |

### TC011: 收货人姓名超长
| 项目 | 内容 |
|------|------|
| 前置条件 | 无 |
| 测试步骤 | 1. 调用 POST /api/address/add 2. rcvrName超过100字符 |
| 预期结果 | 返回验证错误 |
| 优先级 | P1 |

---

## F004 - 更新地址

### TC012: 更新地址成功
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001存在 |
| 测试步骤 | 1. 调用 PUT /api/address/update 2. 传入新数据 |
| 预期结果 | 更新成功，返回新数据 |
| 优先级 | P0 |

### TC013: 更新不存在的地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址not-exist不存在 |
| 测试步骤 | 1. 调用 PUT /api/address/update 2. id=not-exist |
| 预期结果 | 返回错误：address.not.found |
| 优先级 | P0 |

### TC014: 更新他人地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001属于user-001 |
| 测试步骤 | 1. 使用user-002调用 PUT /api/address/update |
| 预期结果 | 返回错误：address.not.found |
| 优先级 | P0 |

---

## F005 - 删除地址

### TC015: 删除非默认地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001为非默认地址 |
| 测试步骤 | 1. 调用 DELETE /api/address/test-addr-001 |
| 预期结果 | 删除成功 |
| 优先级 | P0 |

### TC016: 删除默认地址（有其他地址）
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001为默认地址，还有其他地址 |
| 测试步骤 | 1. 调用 DELETE /api/address/test-addr-001 |
| 预期结果 | 删除成功，最新地址自动设为默认 |
| 优先级 | P0 |

### TC017: 删除默认地址（无其他地址）
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001为唯一地址 |
| 测试步骤 | 1. 调用 DELETE /api/address/test-addr-001 |
| 预期结果 | 删除成功 |
| 优先级 | P1 |

---

## F006 - 设置默认地址

### TC018: 设置默认地址成功
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址test-addr-001存在，当前非默认 |
| 测试步骤 | 1. 调用 PUT /api/address/default/test-addr-001 |
| 预期结果 | 设置成功，原默认地址变为非默认 |
| 优先级 | P0 |

### TC019: 设置不存在的地址为默认
| 项目 | 内容 |
|------|------|
| 前置条件 | 地址not-exist不存在 |
| 测试步骤 | 1. 调用 PUT /api/address/default/not-exist |
| 预期结果 | 返回错误：address.not.found |
| 优先级 | P0 |

---

## F007 - 获取默认地址

### TC020: 缓存命中获取默认地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001有默认地址，缓存存在 |
| 测试步骤 | 1. 调用 GET /api/address/default |
| 预期结果 | 返回默认地址，不查询数据库 |
| 优先级 | P0 |

### TC021: 缓存未命中获取默认地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户user-001有默认地址，缓存不存在 |
| 测试步骤 | 1. 清除缓存 2. 调用 GET /api/address/default |
| 预期结果 | 查询数据库，返回默认地址 |
| 优先级 | P0 |

### TC022: 无默认地址时返回最新地址
| 项目 | 内容 |
|------|------|
| 前置条件 | 用户有地址但无默认地址 |
| 测试步骤 | 1. 调用 GET /api/address/default |
| 预期结果 | 返回最新创建的地址 |
| 优先级 | P1 |

---

## 非功能测试

### TC023: 接口响应时间测试
| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 连续调用100次 GET /api/address/list 2. 统计平均响应时间 |
| 预期结果 | 平均响应时间 < 100ms |
| 优先级 | P1 |

### TC024: 缓存命中率测试
| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 连续调用100次 GET /api/address/list 2. 统计缓存命中次数 |
| 预期结果 | 缓存命中率 > 80% |
| 优先级 | P1 |

### TC025: SQL注入防护测试
| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 在搜索参数中注入SQL语句 |
| 预期结果 | 系统正常处理，无SQL注入风险 |
| 优先级 | P1 |
